# Open-Mic
# OpenMic Bridge — Web Variant  Phone acts as a mic input for Ubuntu with **no Android app** — just a browser tab. Trade-off vs the native client: the browser tab must stay in the foreground with the screen on.
